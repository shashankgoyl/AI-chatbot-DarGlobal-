# Copy to .env for local dev. On Netlify, set this as a Site environment
# variable instead (Site configuration → Environment variables).

# URL of the deployed backend (Render), no trailing slash.
# Local dev default: http://localhost:8000
VITE_API_URL=http://localhost:8000
